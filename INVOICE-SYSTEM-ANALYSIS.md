# تحلیل سیستم فاکتورزدن Hivork

## 1. انواع فاکتور

### فاکتور فروش (Sales Invoice)
- فاکتور نهایی برای فروش قطعی
- دارای وضعیت پرداخت
- دارای وضعیت ارسال
- قابل تبدیل به فاکتور برگشتی

### پیش‌فاکتور (Proforma Invoice)
- فاکتور اولیه برای پیش‌نمایش
- بدون وضعیت پرداخت و ارسال
- قابل تبدیل به فاکتور فروش
- تاریخ انقضا دارد

## 2. اطلاعات مورد نیاز فاکتور

### اطلاعات اصلی
- شماره فاکتور (اتوماتیک و یونیک)
- نوع فاکتور (فروش / پیش‌فاکتور)
- تاریخ صدور (شمسی)
- تاریخ سررسید (اختیاری)
- وضعیت (پیش‌نویس، نهایی، لغو شده، برگشت خورده)

### اطلاعات مشتری
- انتخاب مشتری از لیست
- نمایش اطلاعات کامل مشتری
- نمایش بدهی/بستانکار قبلی

### اطلاعات کسب‌وکار
- نام و لوگوی کسب‌وکار
- اطلاعات تماس
- شماره اقتصادی (اختیاری)

### اقلام فاکتور (محصولات)
- انتخاب محصول یا تنوع محصول
- تعداد/مقدار
- قیمت واحد
- تخفیف ردیفی (اختیاری)
- مالیات ردیفی (اختیاری)
- توضیحات ردیف (اختیاری)

### محاسبات مالی
- جمع مبلغ اقلام (Subtotal)
- تخفیف کل (مبلغ یا درصد)
- مالیات (درصد - پیش‌فرض 9%)
- هزینه‌های اضافی (حمل‌ونقل، بسته‌بندی، بیمه و...)
- مبلغ نهایی قابل پرداخت

### وضعیت‌ها (فقط فاکتور فروش)
- وضعیت پرداخت: پرداخت نشده، پرداخت جزئی، پرداخت کامل
- وضعیت ارسال: ارسال نشده، در حال ارسال، ارسال شده، تحویل داده شده
- تاریخ پرداخت (در صورت پرداخت)
- تاریخ ارسال (در صورت ارسال)

### توضیحات و یادداشت‌ها
- توضیحات کلی فاکتور
- شرایط و ضوابط
- یادداشت‌های داخلی (فقط برای کارمندان)

## 3. ویژگی‌های پیشرفته که باید اضافه شود

### مدیریت موجودی
- کنترل موجودی هنگام ثبت فاکتور
- هشدار در صورت کمبود موجودی
- رزرو موجودی برای پیش‌فاکتور (اختیاری)

### پرداخت و حسابداری
- ثبت پرداخت‌های جزئی (قسطی)
- انواع روش پرداخت (نقد، کارت، چک، اعتباری)
- یادآوری سررسید پرداخت

### چاپ و خروجی
- قالب‌های مختلف فاکتور (رسمی، ساده، مفصل)
- خروجی PDF با امضا و مهر الکترونیکی
- قابلیت ارسال ایمیل و SMS
- بارکد/QR Code برای شناسایی

### گزارش‌گیری
- گزارش فروش روزانه/هفتگی/ماهانه
- گزارش فروش بر اساس محصول
- گزارش فروش بر اساس مشتری
- گزارش مالیات و تخفیفات
- نمودار روند فروش

### جستجو و فیلتر
- جستجو بر اساس شماره فاکتور
- فیلتر بر اساس مشتری
- فیلتر بر اساس وضعیت پرداخت/ارسال
- فیلتر بر اساس بازه تاریخی
- فیلتر بر اساس مبلغ

### عملیات روی فاکتور
- ویرایش فاکتور (فقط پیش‌نویس‌ها)
- کپی فاکتور (برای ساخت فاکتور مشابه)
- لغو فاکتور (با دلیل)
- تبدیل پیش‌فاکتور به فاکتور نهایی
- ثبت فاکتور برگشتی

### اعلان‌ها و یادآوری‌ها
- یادآوری سررسید پرداخت
- یادآوری ارسال کالا
- یادآوری پیگیری مشتری

## 4. ساختار دیتابیس

### جدول Invoice (فاکتورها)
```typescript
- id
- invoiceNumber (یونیک، اتوماتیک)
- type (sales/proforma)
- businessId
- customerId
- issueDate
- dueDate
- status (draft/finalized/cancelled/returned)
- subtotal
- discountType (percentage/amount)
- discountValue
- taxRate
- taxAmount
- extraCostsTotal
- totalAmount
- paymentStatus (unpaid/partial/paid) - فقط sales
- shippingStatus (pending/processing/shipped/delivered) - فقط sales
- notes
- internalNotes
- terms
- createdBy
- createdAt
- updatedAt
```

### جدول InvoiceItem (اقلام فاکتور)
```typescript
- id
- invoiceId
- productId (nullable)
- variantId (nullable)
- name (برای ذخیره نام در زمان صدور)
- description
- quantity
- unitPrice
- discount
- taxRate
- taxAmount
- subtotal
- total
- sortOrder
```

### جدول InvoicePayment (پرداخت‌ها)
```typescript
- id
- invoiceId
- amount
- paymentDate
- paymentMethod (cash/card/check/credit)
- referenceNumber
- notes
- createdBy
- createdAt
```

### جدول InvoiceExtraCost (هزینه‌های اضافی)
```typescript
- id
- invoiceId
- title
- amount
- description
```

## 5. API Endpoints مورد نیاز

### فاکتورها
- `GET /api/invoices` - لیست فاکتورها با فیلتر و صفحه‌بندی
- `GET /api/invoices/:id` - جزئیات یک فاکتور
- `POST /api/invoices` - ایجاد فاکتور جدید
- `PUT /api/invoices/:id` - ویرایش فاکتور
- `DELETE /api/invoices/:id` - حذف فاکتور (soft delete)
- `POST /api/invoices/:id/finalize` - نهایی کردن فاکتور
- `POST /api/invoices/:id/cancel` - لغو فاکتور
- `POST /api/invoices/:id/convert` - تبدیل پیش‌فاکتور به فاکتور
- `GET /api/invoices/:id/pdf` - دانلود PDF فاکتور
- `GET /api/invoices/next-number` - دریافت شماره فاکتور بعدی

### پرداخت‌ها
- `GET /api/invoices/:id/payments` - لیست پرداخت‌های یک فاکتور
- `POST /api/invoices/:id/payments` - ثبت پرداخت جدید
- `DELETE /api/invoices/:id/payments/:paymentId` - حذف پرداخت

### گزارش‌ها
- `GET /api/invoices/reports/summary` - خلاصه آمار فروش
- `GET /api/invoices/reports/by-customer` - گزارش به تفکیک مشتری
- `GET /api/invoices/reports/by-product` - گزارش به تفکیک محصول

## 6. جریان کاری UI/UX

### صفحه لیست فاکتورها
```
┌─────────────────────────────────────────────────────┐
│ 🧾 فاکتورها                          [+ افزودن]     │
├─────────────────────────────────────────────────────┤
│ 🔍 جستجو...  📅 تاریخ  👤 مشتری  💰 مبلغ  📊 وضعیت │
├─────────────────────────────────────────────────────┤
│ شماره │ مشتری │ تاریخ │ مبلغ │ وضعیت │ عملیات       │
│ 1001  │ علی   │ 1404  │ 5M   │ ✓     │ 👁️ 📄 🗑️    │
└─────────────────────────────────────────────────────┘
```

### جریان ایجاد فاکتور
```
1. انتخاب نوع (فروش/پیش‌فاکتور)
   └─> Bottom Sheet با دو گزینه
   
2. انتخاب تاریخ
   └─> Persian Date Picker (دلچسب و مینیمال)
   
3. انتخاب مشتری
   └─> صفحه جدید با لیست + جستجو
   
4. انتخاب محصولات
   └─> صفحه جدید با:
       - لیست محصولات/تنوع‌ها
       - جستجو
       - انتخاب چندتایی با Counter
       - دکمه تایید
       
5. صفحه ثبت فاکتور
   └─> نمایش:
       - اطلاعات مشتری (خلاصه)
       - جدول اقلام با قابلیت ویرایش تعداد
       - محاسبات (تخفیف، مالیات، هزینه اضافی)
       - وضعیت‌ها (برای فاکتور فروش)
       - توضیحات
       - دکمه‌های ذخیره/لغو
```

### صفحه جزئیات فاکتور
```
┌─────────────────────────────────────────────────────┐
│ فاکتور #1001                    [📄 PDF] [✏️ ویرایش] │
├─────────────────────────────────────────────────────┤
│ 📅 تاریخ: 1404/09/02                                │
│ 👤 مشتری: علی احمدی                                 │
│ 📱 تماس: 0912...                                    │
├─────────────────────────────────────────────────────┤
│ اقلام فاکتور:                                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ محصول A  │  5 عدد  │  100,000  │  500,000       │ │
│ │ محصول B  │  2 عدد  │  200,000  │  400,000       │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│ جمع مبلغ:           900,000 ریال                    │
│ تخفیف (10%):       -90,000 ریال                     │
│ مالیات (9%):       +72,900 ریال                     │
│ هزینه حمل:         +50,000 ریال                     │
│ ─────────────────────────────────                   │
│ مبلغ نهایی:        932,900 ریال                     │
├─────────────────────────────────────────────────────┤
│ 💰 وضعیت پرداخت: پرداخت شده ✓                      │
│ 📦 وضعیت ارسال: ارسال نشده                         │
└─────────────────────────────────────────────────────┘
```

## 7. کامپوننت‌های Flutter مورد نیاز

### صفحات اصلی
- `InvoiceListScreen` - لیست فاکتورها
- `InvoiceDetailScreen` - جزئیات فاکتور
- `CreateInvoiceScreen` - ایجاد/ویرایش فاکتور
- `InvoicePreviewScreen` - پیش‌نمایش PDF

### صفحات کمکی
- `CustomerSelectionScreen` - انتخاب مشتری
- `ProductSelectionScreen` - انتخاب محصولات
- `InvoiceReportsScreen` - گزارش‌ها

### ویجت‌های سفارشی
- `InvoiceCard` - کارت فاکتور در لیست
- `InvoiceItemTable` - جدول اقلام
- `InvoiceCalculationPanel` - پنل محاسبات
- `InvoiceStatusBadge` - نشان وضعیت
- `PersianDatePicker` - تقویم شمسی
- `PaymentMethodSelector` - انتخاب روش پرداخت
- `DiscountInput` - ورودی تخفیف (مبلغ/درصد)

### مدل‌ها
- `Invoice` - مدل فاکتور
- `InvoiceItem` - مدل ردیف فاکتور
- `InvoicePayment` - مدل پرداخت
- `InvoiceExtraCost` - مدل هزینه اضافی

### سرویس‌ها
- `InvoiceService` - مدیریت APIها
- `InvoicePdfService` - تولید PDF
- `InvoiceCalculationService` - محاسبات

## 8. اولویت‌بندی پیاده‌سازی

### فاز 1 - Backend (Core)
1. ایجاد Entity ها
2. ایجاد DTOها
3. پیاده‌سازی CRUD فاکتورها
4. محاسبات مالی
5. مدیریت اقلام فاکتور

### فاز 2 - Backend (Advanced)
1. مدیریت پرداخت‌ها
2. گزارش‌ها و آمار
3. PDF Generator
4. کنترل موجودی

### فاز 3 - Flutter (Core)
1. مدل‌ها و سرویس‌ها
2. لیست فاکتورها
3. جزئیات فاکتور
4. ایجاد فاکتور ساده

### فاز 4 - Flutter (Advanced)
1. انتخاب محصولات پیشرفته
2. محاسبات پویا
3. پیش‌نمایش PDF
4. گزارش‌ها

## 9. نکات طراحی UI/UX

### تم سراسری
- رنگ اصلی: سبز (#4CAF50) برای موفقیت
- رنگ قرمز: (#F44336) برای خطا/لغو
- رنگ آبی: (#2196F3) برای اطلاعات
- فونت: IRANSans
- Border Radius: 12px
- Spacing: 16px

### Dark Mode
- Background: #121212
- Surface: #1E1E1E
- Card: #2C2C2C

### Light Mode
- Background: #F5F5F5
- Surface: #FFFFFF
- Card: #FAFAFA

### Animation
- Fade In/Out: 200ms
- Slide: 300ms
- Scale: 150ms

## 10. اولویت امنیت

- اعتبارسنجی داده‌ها در Backend
- محدودیت دسترسی بر اساس نقش کاربر
- لاگ تمام تغییرات فاکتورها
- جلوگیری از ویرایش فاکتورهای نهایی شده
- رمزنگاری PDF ها (اختیاری)

---

**این تحلیل پایه و اساس سیستم فاکتورزدن ماست. حالا شروع به پیاده‌سازی می‌کنیم! 🚀**
